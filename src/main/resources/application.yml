spring:
  application:
    name: lookkit2nd
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  config:
    import: optional:file:.env[.properties]
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  http:
    encoding:
      charset: UTF-8
      enabled: true
  mvc:
    static-path-pattern: "/**"
  mail:
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 90b8311d4ff255bb3b80bf50cf04708f
            client-secret: HOZfi3b3K2IFkavM5fDEwRGYgV9aLoqW
            scope:
              - profile_nickname
              - account_email
              - name
              - gender
              - birthday
              - birthyear
              - phone_number
            redirect-uri: "http://localhost:8081/kakao-login"
            client-name: kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

server:
  port: 8081
  servlet:
    encoding:
      force-response: true
      charset: UTF-8
    session:
      timeout: 60m
      cookie:
        max-age: 60m

logging:
  level:
    org:
      springframework:
        web: DEBUG
    org.springframework.web.servlet.DispatcherServlet: DEBUG